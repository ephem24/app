import React from 'react';

const Header = () => {
  return (
    <header className="flex h-16 items-center justify-between border-b bg-white px-4 md:px-6">
      <h1 className="text-lg font-semibold">AI Tutor Dashboard</h1>
      {/* Add any other header elements here, like user profile, navigation, etc. */}
    </header>
  );
};

export default Header;
